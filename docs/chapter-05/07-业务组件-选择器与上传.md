# 5.7 业务组件：选择器与上传

## 本节目标

- ✅ 封装用户选择器
- ✅ 封装图片上传组件
- ✅ 封装富文本编辑器

---

## 1. 用户选择器

**src/components/UserSelector.vue**:

```vue
<script setup lang="ts">
import { ref } from 'vue'
import { userApi } from '@/api'

const users = ref([])
const loading = ref(false)

const fetchUsers = async (keyword: string) => {
  loading.value = true
  try {
    const res = await userApi.getList({ keyword })
    users.value = res.items
  } finally {
    loading.value = false
  }
}
</script>

<template>
  <n-select
    :loading="loading"
    :options="users"
    filterable
    @search="fetchUsers"
  />
</template>
```

---

## 2. 图片上传

**src/components/ImageUpload.vue**:

```vue
<script setup lang="ts">
import { ref } from 'vue'

const fileList = ref([])

const handleUpload = async (options: any) => {
  const { file } = options
  const formData = new FormData()
  formData.append('file', file.file)

  try {
    const res = await request.post('/upload', formData, {
      headers: { 'Content-Type': 'multipart/form-data' },
    })
    file.url = res.url
  } catch (error) {
    console.error('上传失败:', error)
  }
}
</script>

<template>
  <n-upload
    v-model:file-list="fileList"
    :custom-request="handleUpload"
    list-type="image-card"
    accept="image/*"
  >
    点击上传
  </n-upload>
</template>
```

---

## 3. 富文本编辑器

使用 Tiptap 或 Quill：

```vue
<script setup lang="ts">
import { ref } from 'vue'
import { QuillEditor } from '@vueup/vue-quill'
import '@vueup/vue-quill/dist/vue-quill.snow.css'

const content = ref('')
</script>

<template>
  <QuillEditor
    v-model:content="content"
    content-type="html"
    theme="snow"
  />
</template>
```

---

## 4. 本节小结

✅ 完成的工作：
- 封装了用户选择器
- 封装了图片上传组件
- 集成了富文本编辑器

**下一步**: [5.8 组件文档与类型定义](./08-组件文档与类型定义.md)
