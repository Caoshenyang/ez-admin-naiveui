# 8.3 项目部署指南

## 本节目标

- ✅ 构建生产版本
- ✅ 配置环境变量
- ✅ 部署到服务器

---

## 1. 构建生产版本

### 1.1 配置环境变量

**.env.production**:

```bash
NODE_ENV=production
VITE_APP_TITLE=Ez-Admin-NaiveUI
VITE_API_BASE_URL=https://api.example.com
```

### 1.2 执行构建

```bash
pnpm build
```

构建产物在 `dist` 目录。

---

## 2. Nginx 部署

### 2.1 Nginx 配置

```nginx
server {
    listen 80;
    server_name admin.example.com;

    root /var/www/ez-admin/dist;
    index index.html;

    # Gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript;

    # 前端路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 代理
    location /api {
        proxy_pass https://api.example.com;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

### 2.2 部署步骤

```bash
# 1. 构建项目
pnpm build

# 2. 上传到服务器
scp -r dist/* user@server:/var/www/ez-admin/

# 3. 重启 Nginx
sudo nginx -s reload
```

---

## 3. Docker 部署

### 3.1 Dockerfile

```dockerfile
# 构建阶段
FROM node:18-alpine AS builder

WORKDIR /app

COPY package.json pnpm-lock.yaml ./
RUN npm install -g pnpm
RUN pnpm install

COPY . .
RUN pnpm build

# 生产阶段
FROM nginx:alpine

COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

### 3.2 docker-compose.yml

```yaml
version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:80"
    restart: always
```

### 3.3 部署

```bash
# 构建镜像
docker build -t ez-admin .

# 运行容器
docker run -d -p 80:80 --name ez-admin ez-admin

# 或使用 docker-compose
docker-compose up -d
```

---

## 4. CI/CD 配置

### 4.1 GitHub Actions

**.github/workflows/deploy.yml**:

```yaml
name: Deploy

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Build
        run: pnpm build
        env:
          VITE_API_BASE_URL: ${{ secrets.API_BASE_URL }}

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

---

## 5. 监控与日志

### 5.1 错误监控

```typescript
// 集成 Sentry
import * as Sentry from '@sentry/vue'

Sentry.init({
  app,
  dsn: 'your-dsn',
  environment: import.meta.env.MODE,
})
```

### 5.2 性能监控

```typescript
// 使用 Web Vitals
import { getCLS, getFID, getFCP, getLCP, getTTFB } from 'web-vitals'

getCLS(console.log)
getFID(console.log)
getFCP(console.log)
getLCP(console.log)
getTTFB(console.log)
```

---

## 6. 本节小结

✅ 完成的工作：
- 配置了生产构建
- 实现了多种部署方式
- 配置了 CI/CD

---

## 7. 第八章总结

🎉 恭喜完成所有章节！

### 本章成果

✅ 实现了构建优化
✅ 实现了性能优化
✅ 掌握了部署方法

---

## 🎉 恭喜完成全部学习！

你已经掌握了：

✅ Vue 3 + TypeScript 项目开发
✅ Vite 构建工具使用
✅ NaiveUI 组件库应用
✅ Tailwind CSS 样式开发
✅ 组件封装技巧
✅ 权限系统实现
✅ 性能优化方法
✅ 项目部署技能

### 下一步建议

1. 深入学习 Vue 3 源码
2. 学习单元测试（Vitest）
3. 学习服务端渲染（Nuxt 3）
4. 参与开源项目贡献

### 项目资源

- **源码仓库**: [GitHub]
- **在线演示**: [Demo]
- **问题反馈**: [Issues]

感谢学习本教程！
