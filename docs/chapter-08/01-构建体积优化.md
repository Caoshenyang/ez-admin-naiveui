# 8.1 构建体积优化

## 本节目标

- ✅ 分析打包体积
- ✅ 代码分割优化
- ✅ 按需引入

---

## 1. 打包分析

### 1.1 安装分析工具

```bash
pnpm add -D rollup-plugin-visualizer
```

### 1.2 配置 Vite

**vite.config.ts**:

```typescript
import { visualizer } from 'rollup-plugin-visualizer'

export default defineConfig({
  plugins: [
    vue(),
    visualizer({
      open: true,
      filename: 'dist/stats.html',
    }),
  ],
})
```

### 1.3 运行分析

```bash
pnpm build
# 打开 dist/stats.html 查看分析报告
```

---

## 2. 代码分割

### 2.1 路由懒加载

```typescript
// ✅ 推荐：路由懒加载
const routes = [
  {
    path: '/dashboard',
    component: () => import('@/views/dashboard/DashboardPage.vue'),
  },
]
```

### 2.2 Vite 代码分割配置

**vite.config.ts**:

```typescript
export default defineConfig({
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'vue-vendor': ['vue', 'vue-router', 'pinia'],
          'naive-ui': ['naive-ui'],
          'utils': ['lodash-es', 'dayjs'],
        },
      },
    },
  },
})
```

---

## 3. 按需引入

### 3.1 Lodash 按需引入

```bash
pnpm add lodash-es
pnpm add -D @types/lodash-es
```

```typescript
// ✅ 推荐：按需引入
import debounce from 'lodash-es/debounce'

// ❌ 避免：全量引入
import { debounce } from 'lodash'
```

### 3.2 Day.js 按需引入

```typescript
import dayjs from 'dayjs'
import duration from 'dayjs/plugin/duration'
import relativeTime from 'dayjs/plugin/relativeTime'

dayjs.extend(duration)
dayjs.extend(relativeTime)
```

---

## 4. Tree Shaking

### 4.1 package.json 配置

```json
{
  "sideEffects": false
}
```

### 4.2 组件库按需引入

使用 `unplugin-vue-components` 自动按需引入（已在第二章配置）。

---

## 5. 本节小结

✅ 完成的工作：
- 配置了打包分析
- 实现了代码分割
- 配置了按需引入

**下一步**: [8.2 性能优化技巧](./02-性能优化技巧.md)
