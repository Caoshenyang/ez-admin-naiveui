/**
 * 用户管理配置统一导出
 * 约定：各配置文件分开存放，这里只负责统一导出
 */
export { userTableConfig, userActionButtons } from './userTableConfig'
export { createUserFormConfig } from './userFormConfig'
export { userCrudConfig } from './userCrudConfig'
